pipeline {
    agent any

    stages {
        stage('CheckValidate') {
            steps {
                echo 'Iam validating the code'
            }
        }
stage('Install') {
            steps {
                echo 'Iam installing the code'
            }
        }
stage('Deploy') {
            steps {
                echo 'Iam Deploying the code'
            }
        }
    }
}
